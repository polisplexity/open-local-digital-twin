'use client';
import MainLayout from '@/layout/workspace-layout'

const AppsLayout = ({ children }) => {

    return (
        <MainLayout>
            {children}
        </MainLayout>
    )
}

export default AppsLayout
