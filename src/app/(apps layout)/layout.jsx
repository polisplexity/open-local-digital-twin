'use client';
import MainLayout from '@/layout/apps-layout'

const AppsLayout = ({ children }) => {

    return (
        <MainLayout>
            {children}
        </MainLayout>
    )
}

export default AppsLayout
